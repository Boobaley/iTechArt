const formTitle = {
    fontSize: 70,
    fontWeight: 800,
    color: 'red',
    marginBottom: 10,
    textAlign: 'center'
}

const input = {
    borderRadius: 20,
    border: '3px solid #a0a0a0',
    backgroundColor: 'white',
    padding: 20,
    marginRight: 20,
    outline: 'none',
    fontWeight: 600,
      lastOfType: {
        borderRadius: 20,
        border: '3px solid #a0a0a0',
        backgroundColor: 'white',
        padding: 20,
        outline: 'none',
        fontWeight: 600,
        marginRight: 0
    } 
}

const form = {
    width: 'max-content',
    margin: '0 auto 30px auto',
    display: 'flex',
    flexDirection: 'column'
}

const button = {
    margin: '20px auto 0 auto',
    width: 200,
    border: '3px solid red',
    fontWeight: 800,
    borderRadius: 50,
    color: 'black'
}

const outputData = {
    margin: '0 auto',
    width: '50vw',
    border: '3px solid  #3f51b5',
    borderRadius: 20,
    padding: 100,
    // boxShadow: 'inset 0px 0px 4px 9px #a0a0a0'
}

export  { input, form, formTitle, button, outputData };